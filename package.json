{
  "name": "algo-ts",
  "version": "0.0.1",
  "description": "Using typescript to implement the algorithm",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "esbuild": "^0.8.50"
  }
}
